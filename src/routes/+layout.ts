// Prerender every page to static HTML so social crawlers (which don't run JS)
// see each page's own title/description/share image. The blog posts are
// discovered by crawling the links on /blog.
export const prerender = true;
